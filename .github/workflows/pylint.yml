---
name: "linting - ruff"

"on":
  push:
    branches:
      - main # Set a branch to deploy
  pull_request:

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: jdx/mise-action@v3
      - name: Running ruff
        run: |
          mise run ruff
